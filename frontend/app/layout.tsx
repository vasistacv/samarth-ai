import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Samarth AI - Advanced Agricultural Intelligence",
  description: "AI-powered agricultural insights with voice assistance, rainfall predictions, and crop analytics",
  keywords: "agriculture, AI, rainfall, crops, voice assistant, analytics",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
